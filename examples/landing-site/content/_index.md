---
layout: home
---

The home page is fully assembled from `site.toml` section blocks; the
markdown body of `_index.md` is intentionally empty. (The home layout
doesn't render `.content` — every visible string lives in a section
partial driven by frontmatter.)
