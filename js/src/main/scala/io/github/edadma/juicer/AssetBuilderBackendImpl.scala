package io.github.edadma.juicer

/** Scala.js stub: same degradation story as Scala Native — the asset
  * pipeline falls back to verbatim source copies and the build still
  * succeeds. */
def newAssetBuilderBackend(): AssetBuilderBackend = AssetBuilderBackend.Unavailable
