package io.github.edadma.juicer

/** Scala Native stub: the asset pipeline degrades to verbatim source
  * copies. When `cross_platform` grows a `runCommand` helper, this can
  * be promoted to a real implementation against the same trait without
  * touching the shared pipeline. */
def newAssetBuilderBackend(): AssetBuilderBackend = AssetBuilderBackend.Unavailable
